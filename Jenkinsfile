pipeline {
  environment {
    AWS_CRED=credentials('nkwochidubem_aws_cred')  
  }
  stages {
    stage('checkout repo') {
      steps {
        '''
          sh 'echo Hi chidubem'


      }
    }
  }
}
